(async()=>{const allRows=[];const headers=[...document.querySelectorAll(".sixtyfour-data-table thead th")].map(th=>{const btn=th.querySelector("button span")||th.querySelector("span");return btn?btn.textContent.trim():""});console.log("Headers: "+headers.join(", "));let page=1;while(true){const rows=document.querySelectorAll(".sixtyfour-data-table tbody tr");rows.forEach(row=>{const cells=[...row.querySelectorAll("td")].map(td=>{const link=td.querySelector("a");return link?link.textContent.trim():td.textContent.trim()});if(cells.length>0)allRows.push(cells)});console.log("Page "+page+" done - "+allRows.length+" rows total");const nextBtn=document.querySelector('button[aria-label="Next"]');if(nextBtn&&!nextBtn.disabled){nextBtn.click();await new Promise(r=>setTimeout(r,2000));page++}else{break}}const csvHeader=headers.join(",");const csvRows=allRows.map(row=>row.map(cell=>'"'+cell.replace(/"/g,'""')+'"').join(","));const csv=[csvHeader,...csvRows].join("\n");const blob=new Blob([csv],{type:"text/csv"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download="64analytics_team_pitching_2026.csv";a.click();URL.revokeObjectURL(url);console.log("Done! Downloaded "+allRows.length+" rows.")})();
