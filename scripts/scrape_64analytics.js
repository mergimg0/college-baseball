(async()=>{const allRows=[];const totalPages=104;const headers=[...document.querySelectorAll(".sixtyfour-data-table thead th")].map(th=>{const btn=th.querySelector("button span")||th.querySelector("span");return btn?btn.textContent.trim():""});console.log("Headers: "+headers.join(", "));console.log("Starting scrape of "+totalPages+" pages...");for(let page=1;page<=totalPages;page++){const rows=document.querySelectorAll(".sixtyfour-data-table tbody tr");rows.forEach(row=>{const cells=[...row.querySelectorAll("td")].map(td=>{const link=td.querySelector("a");return link?link.textContent.trim():td.textContent.trim()});if(cells.length>0)allRows.push(cells)});console.log("Page "+page+"/"+totalPages+" done - "+allRows.length+" rows total");if(page<totalPages){const nextBtn=document.querySelector('button[aria-label="Next"]');if(nextBtn&&!nextBtn.disabled){nextBtn.click();await new Promise(r=>setTimeout(r,2000))}else{console.log("No more pages");break}}}const csvHeader=headers.join(",");const csvRows=allRows.map(row=>row.map(cell=>'"'+cell.replace(/"/g,'""')+'"').join(","));const csv=[csvHeader,...csvRows].join("\n");const blob=new Blob([csv],{type:"text/csv"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download="64analytics_pitcher_stats_2026.csv";a.click();URL.revokeObjectURL(url);console.log("Done! Downloaded "+allRows.length+" rows.")})();
